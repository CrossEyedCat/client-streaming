import React from "react";
import "./style.css";

export const Fild = ({ className }) => {
    return (
        <div className={`fild ${className}`}>
            <div className="text-wrapper-3">Имя</div>

            <div className="div-2" />
        </div>
    );
};
